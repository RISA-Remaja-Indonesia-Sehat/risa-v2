import ArticlePageWrapper from '@/app/components/first-time/ArticlePageWrapper';

export default function ArticleLayout({ children }) {
  return <ArticlePageWrapper>{children}</ArticlePageWrapper>;
}
